from platform import node
"""LinkedList module."""

from Node import Node

class LinkedList:
    def __init__(self):
        self.node = Node()
        self.head = node
        self.tail = node
        self.length = 1
